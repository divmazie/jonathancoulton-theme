{% macro warning(text,text2) %}
    <span style="color: red;">{{ text|default('Warning!') }}{{ text2 }}</span>
{% endmacro %}
{% import _self as macros %}
{% include 'html-header.twig' %}
	{% block head %}
    {% endblock %}
    <style>
        .admin_album, .admin_zip, .admin_track, .admin_encode, .admin_bonus {
            margin-left: 20px;
        }
        .admin_album, .admin_track, .admin_bonus {
            padding: 5px;
        }
        .admin_album {
            margin-bottom: 20px;
            background-color: #ddd;
        }
        .admin_track {
            margin-bottom: 10px;
            background-color: #bbb;
        }
    </style>
    </head>

    <body class="{{body_class}}" data-template="base.twig">
        <div class="container">
            This is the music admin page!
            {% for album in albums %}
                <div class="admin_album">
                    Title: {{ album.title }} <br />
                    Artist: {{ album.artist }} <br />
                    Art: {% if album.art.exists %} {{ album.art.filename }} {% else %} {{ macros.warning('ART MISSING!') }} {% endif %} <br />
                    {% if not album.encode_worthy %} {{ macros.warning('NOT ENCODE WORTHY!') }} <br /> {% endif %}
                    {% if not album.show_album %} {{ macros.warning('NOT MARKED AS READY!') }} <br /> {% endif %}
                    Zips:
                    {% for zip in album.album_zips %}
                        <div class="admin_zip">
                            {% if zip.exists %}{{ zip.format }} zip good to go{% endif %}
                            {% if not zip.zip_worthy %} {{ macros.warning(zip.format," zip not zip-worthy!") }} {% endif %}
                            {% if zip.missing_encodes %} {{ macros.warning(zip.format," encodes missing!") }} {% endif %}
                        </div>
                    {% endfor  %}
                    Tracks:
                    {% for track in album.tracks %}
                        <div class="admin_track">
                            Title: {{ track.title }} <br />
                            Artist: {{ track.artist }} <br />
                            Art: {% if track.art.exists %} {{ track.art.filename }} {% else %} {{ macros.warning('ART MISSING!') }} {% endif %} <br />
                            Source: {% if track.source.exists %} {{ track.source.filename }} {% else %} {{ macros.warning('SOURCE MISSING!') }} {% endif %} <br />
                            {% if not track.encode_worthy %} {{ macros.warning('NOT ENCODE WORTHY!') }} <br /> {% endif %}
                            Encodes:
                            {% for encode in track.encodes %}
                                <div class="admin_encode">
                                    {% if encode.exists %}{{ encode.format }} encode good to go{% endif %}
                                    {% if not encode.exists %} {{ macros.warning(encode.format," encode missing!") }} {% endif %}
                                </div>
                            {% endfor  %}
                        </div>
                    {% endfor %}
                    Bonus assets:
                    {% if album.bonus_assets|length == 0 %}
                        <div class="admin_bonus">No bunus assets.</div>
                    {% endif %}
                    {% for bonus in album.bonus_assets %}
                        <div class="admin_bonus">
                            Source: {% if bonus.exists %} {{ bonus.filename }} {% else %} {{ macros.warning('SOURCE MISSING!') }} {% endif %} <br />
                        </div>
                    {% endfor %}
                </div> <br />
            {% endfor %}
            <pre>{{ albums|print_r }}</pre>
        </div>
    </body>
</html>
